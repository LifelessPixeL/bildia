# config/routes.yaml
api:
  path: /api
  controller: Symfony\Bundle\FrameworkBundle\Controller\TemplateController
  defaults:
    # the path of the template to render
    template: 'api/index.html.twig'

    # the response status code (default: 200)
    statusCode: 200
